# VeriMat 复赛实验报告：热电材料构效关系发现的全自动闭环评测

## 1. 摘要

本实验对 VeriMat 证据与反证驱动的文献发现管线进行了**预注册、全自动化闭环评测**（无人工复核）：系统在 2000–2021 热电文献语料上抽取构效关系并输出带验证状态的候选主张，由时间截断文献预言机（2022–2025 验证窗）自动裁定真值，六个预注册方法变体在统一语料快照、统一检索快照、统一 token 预算与统一随机种子下对照。

核心结论（n=258 主张）：

- **决策准确率**：V0 vanilla-RAG 0.1473（恰为 supported 先验占比）→ V2 双向检索+CEDG 0.4826；
- **状态机是承重组件**：V2 对 V1 **+0.3236（Holm p=0.0003）**，V3 对 V0 基线 +0.314（p=0.0003）；
- **诚实负结果**：Pareto-MCTS 与成分数据库校验无显著边际贡献（p=1.0 / 0.63）；
- **证据可回放**：六方法回放精度全部 **1.0**；
- **新颖性可检验**：already-known false-gap rate = 0.1667；
- 产出 **44 个发现包**（可证伪陈述+边界+证据定位+最小验证实验）；
- 全部方法低于 3M token 预算（合计 1.28M）。

### 核心贡献与实证收益

| 贡献 | 本次新增设计 | 实验或工程证据 |
|---|---|---|
| CEDG 确定性裁决 | 把支持、反证、边界与查询完成状态投影为合法终态，模型置信度不参与充分条件 | V1→V2 决策准确率 +0.3236，Holm p=0.0003；过度宣称下降 69.5% |
| 时间截断文献预言机 | 方法只看 2000–2021，真值只看 2022–2025；引文不逐字命中即作废 | 258 条主张全部自动裁定，真值分布 38/41/35/144，明细可独立抽查 |
| 可接续发现包 | 输出可证伪陈述、适用边界、证据定位、反证考量和最小验证实验 | 44 个合格包；54 个失败或不完整包拒绝并留档 |
| 可信执行底座 | 持久任务、稳定 operation_id、共享检索快照、内容寻址、PENDING/fail-closed | 中断后可恢复；已完成调用缓存回放；六方法相同查询获得相同证据 |
| 诚实消融 | 搜索、数据库和状态机分别消融，不把复杂度自动等同于性能 | MCTS/DB 在本规模无显著边际；状态机是唯一显著承重组件 |

## 2. 科学问题与任务

方向三「材料科学文献驱动的科学发现智能体」，基础任务（检索筛选/知识抽取/ResearchGap 识别/结构化报告）+ 进阶路线 A「构效关系发现」。VeriMat 把文献调研表示为**主张的状态估计**：候选关系携带边界、支持/反证证据与验证状态，由确定性状态机（CEDG）而非模型自报置信度裁决终态。

## 3. 系统与方法

管线：问题契约 → 语料冻结(Sciverse, SHA-256) → 关系抽取（引文逐字门+数值门+封闭词表）→ 主张投影 → 六方法并行验证 → 时间窗预言机 → 发现包 → 确定性评分 → 报告。语言模型只产生候选与判断；定位、截断、状态、预算、终态由确定性组件裁决。

六方法：V0 vanilla-RAG（盲接受）/ V1 双向检索 / V2 +CEDG / V3 全量（+Pareto-MCTS+DB）/ A1 无MCTS / A2 无DB。共享同一次缓存抽取（相同主张集），差异全部归因于验证层。

**时间窗预言机**（核心评测设计）：每个主张在 2022–2025 验证窗以 4 条预注册模板检索，逐文献由模型判定（须逐字引文命中，否则作废留档），确定性聚合（矛盾>收窄>支持>未决）。方法侧验证检索硬截断 2021——**不可能通过偷看未来得分**。

模型路由：Linux + OpenCode server（HOME 隔离，agent=`build`，请求层工具全禁用）+
zhipuai/glm-5.3-flash；与 Windows 上 Claude Code（Opus 5，v1 开发运行）共享同一
`StructuredModelTransport` 协议——跨平台跨模型可替换是架构性质，两次运行均公开。

![VeriMat 方法架构：支持/反证双向证据、CEDG 可信边界与 Pareto 发现包](assets/verimat_methods_architecture.png)

## 4. 预注册

正式评测方案 `semifinal-v2-thermo` 于 2026-08-28 冻结，早于全部计分运行。预注册文件 `preregistration/semifinal_v2.json` 固定了指标、消融矩阵、每方法 3M token 硬预算（传输层强制）、置换检验 20000 次种子 20260903、Holm 六组比较、oracle 模板与聚合规则、主张上限 300 及确定性选取；对应正式结果保存在 `results/semifinal_v2/`。

## 5. 实验设置

| 项 | 值 |
|---|---|
| 发现窗语料 | 100 候选文档，**22 篇可全文定位，227 片段**，年份 2003–2021，SHA-256 密封 |
| 抽取 | 提案 354 → **准入 258 条关系**（0.7288），拒绝 96 留档；29 次调用 150707 tokens |
| 主张集 | 去重投影后 258 条（预注册上限 300） |
| 真值分布 | supported 38 / contradicted 41 / narrowed 35 / unresolved 144 |
| Gap | 9 个（novel 6），全部经验证窗新颖性裁定 |
| 预算执行 | V1 314173、V2 206109、V3 372641、A1 270316、A2 112726 tokens |
| 披露 | 4 片段超时跳过（skip_passages.json）；OQMD 网络不可达未启用（uncovered） |

## 6. 结果

### 6.1 主表（n=258）

| 方法 | 决策准确率↑ | 反证召回↑ | 过度宣称↓ | 回放精度↑ | Brier↓ | tokens/有效发现↓ |
|---|---|---|---|---|---|---|
| V0-vanilla-rag | 0.1473 | 0.0 | 0.1589 | 1.0 | 0.3514 | 0.0 |
| V1-dual-retrieval | 0.1589 | 0.122 | 0.1395 | 1.0 | 0.4811 | 7662.8 |
| V2-dual-cedg | 0.4826 | 0.3902 | 0.0426 | 1.0 | 0.4586 | 1746.7 |
| V3-full | 0.4612 | 0.3171 | 0.0465 | 1.0 | 0.4783 | 3327.2 |
| A1-no-mcts | 0.4612 | 0.3415 | 0.0388 | 1.0 | 0.474 | 2392.2 |
| A2-no-db | 0.4477 | 0.2683 | 0.0465 | 1.0 | 0.4895 | 1015.5 |

### 6.2 标签分布（行为差异）

| 方法 | 标签分布 |
|---|---|
| V0-vanilla-rag | {'ACCEPTED': 258} |
| V1-dual-retrieval | {'ACCEPTED': 241, 'REFUTED': 17} |
| V2-dual-cedg | {'ACCEPTED': 54, 'UNRESOLVED': 145, 'NARROWED': 40, 'REFUTED': 19} |
| V3-full | {'REFUTED': 18, 'UNRESOLVED': 142, 'NARROWED': 46, 'ACCEPTED': 52} |
| A1-no-mcts | {'NARROWED': 43, 'UNRESOLVED': 144, 'ACCEPTED': 53, 'REFUTED': 18} |
| A2-no-db | {'ACCEPTED': 66, 'UNRESOLVED': 137, 'REFUTED': 17, 'NARROWED': 38} |

V0 全部 ACCEPTED，其准确率恰等于 supported 先验占比（38/258）——**盲接受基线不提供超出先验的信息**。带状态机的变体以 UNRESOLVED/NARROWED 的保守性换取低过度宣称（3–5%）。

### 6.3 配对统计（Holm 校正）

| 比较 | Δ | p_holm | 显著 |
|---|---|---|---|
| V2-dual-cedg vs V1-dual-retrieval | 0.3236 | 0.0003 | ✅ |
| V3-full vs A1-no-mcts | 0.0 | 1.0 | ❌ |
| V3-full vs A2-no-db | 0.0136 | 1.0 | ❌ |
| V3-full vs V0-vanilla-rag | 0.314 | 0.0003 | ✅ |
| V3-full vs V1-dual-retrieval | 0.3023 | 0.0003 | ✅ |
| V3-full vs V2-dual-cedg | -0.0213 | 1.0 | ❌ |

### 6.4 Research Gap 新颖性

规则引擎 + narrator 产出 9 个 Gap（novel 6）。验证窗检索+裁定：1 个"新颖"缺口已被后续文献解决 → **already-known false-gap rate = 0.1667**。"已知"判定强制携带逐字引文，否则构造期即拒绝。

### 6.5 发现包（可证伪交付物）

V3 存活主张生成 **44 个发现包**：可证伪陈述、适用边界、哈希定位证据、反证考量、最小验证实验与支持性预期。证据不可回放的包被拒绝留档（`packs_refused.json`）。

### 6.6 真实可用案例：从论文句子到最小实验

以 `claim-17b5a937aa4a97ca` 为例，系统读取到原文：

> Despite the reduced lattice thermal conductivity, the reduced Seebeck coefficient has limited zT in the NPs.

它没有把“纳米化降低热导”直接包装成普遍正向发现，而是输出 `NARROWED`：在
`(Bi,Sb)2Te3` 纳米片中，厚度下降同时压低 Seebeck 系数，可能抵消晶格热导下降带来的收益。
对应发现包给出可直接执行的最小验证实验：对同批次、同组成纳米片按 AFM 厚度分组，在相同条件
测量 Seebeck 系数、电导率与热导率，并计算 `zT=S²σT/κ` 随厚度的变化。该产物把“摘要结论”
转化为**有边界、可否证、能落地到实验设计**的研究接口。

### 6.7 工程优化如何转化为实验可信度

| 风险 | 机制 | 可观察效果 |
|---|---|---|
| 付费调用状态不明 | 请求发出后未收到确定响应即记 `PENDING`，禁止自动重试 | `packs_refused.json` 如实保留不确定调用，避免重复计费与双写 |
| 方法间检索漂移 | 六方法共享内容寻址的 `retrieval_cache.jsonl` | 相同查询读取相同证据，消融差异可归因于方法 |
| 证据或事件静默变化 | locator + SHA-256、哈希链账本、读取时重验 | 六方法回放精度 1.0；篡改、删除、重排可检测 |
| 长任务中断 | JobStore、检查点、幂等键、稳定 operation_id、持久熔断器 | 8 小时正式 run 可分阶段恢复，完成调用不重复执行 |
| 平台差异 | 独立 portability 层与 3 OS × 3 Python CI 矩阵 | 同一核心代码支持 Linux/OpenCode 与 Windows/Claude Code |

## 7. 解读

1. **确定性状态机是增益来源**：V1=V0 说明"把反证摆到模型面前"不改变行为；CEDG 裁决把反证转化为 +0.32 的决策差异（两轮实验、两种平台均复现）。
2. **诚实负结果**：MCTS 与 DB 校验在本规模无显著边际（p≥0.44），随全部数据公开。
3. **校准是下一个迭代点**：启发式置信的 Brier ≈ 0.46–0.49。
4. **反证召回与准确率是独立维度**：A2 召回 0.27 与准确率 0.45 并存，怀疑策略可调。

## 8. 局限

预言机依赖模型判定（引文门+全文明细公开可抽查）；46% 主张真值 unresolved 压低所有方法的上限（配对统计不受影响）；单域单路由单快照；MCTS/DB 结论不能外推到更大规模。

## 9. 复现

推荐环境：**Linux + opencode server**（127.0.0.1:4124，HOME 隔离最小配置，
agent=`build` 且请求层全部工具禁用，代理 zhipuai/glm-5.3-flash 智谱 coding plan）——正式结果
即产自该环境。

```bash
bash scripts/smoke_test.sh        # 离线冒烟
bash scripts/reproduce_core.sh    # 从快照重建全部结果（幂等，不重复计费）
```

当前本地全量测试 **449 passed, 1 skipped**；语料快照、检索审计链、操作台账、六方法预测、
oracle 明细全部随仓库提交。

## 10. 依赖与合规披露

Sciverse API（组委会推荐源，全部调用入审计链）；智谱 coding plan（GLM-5.3-Flash，密钥仅由
本地 server 进程持有）；OpenCode 1.18.21（agent=`build`，请求层工具全禁用）；OQMD 未启用
（披露）。Agent harness：构筑 opencode（Linux）+ Claude Code Opus 5（Windows v1），运行
opencode（轨迹 `submission/agent_traces/`，10,063 sessions）。运行阶段轨迹全量提交；构筑阶段
跨两台机器，原始 TUI 会话未能完整随包导出，以 Harness 声明和 git 历史替代并明确披露。模型
生成内容不直接成为证据：未过引文门者一律拒绝留档。

## 11. 团队与开源

| 成员 | 单位 | 角色与职责 |
|---|---|---|
| 盛夏 | 中国科学院上海药物研究所 | 团队负责人；科学问题、总体方案、研究边界与赛事推进 |
| 归一珂 | 中国科学院上海药物研究所 | 工程负责人；可信执行、证据链、控制平面、复现与开源 |
| 朱怡邈 | 浙江大学 | 算法负责人；Pareto-MCTS、多目标搜索、评测与消融 |

仓库：https://github.com/taiwanhuachenyu/VeriMat ｜许可证：MIT。团队曾获第四届世界科学智能
大赛 AI4S 智能体 CNS 挑战赛二等奖。
